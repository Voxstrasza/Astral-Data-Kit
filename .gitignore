# Dependencies and build output
node_modules/
dist/
build/icon.ico

# The Windows launcher the packager regenerates
Astral.lnk

# Icons you have added yourself; the app keeps its own copies outside the repo
custom_icon/

# Local scratch
*.log
.DS_Store
Thumbs.db

# The Armory's backdrop screenshots are dropped in art/creation and served from
# public/art/creation. The two copies are identical, so only the served one is kept:
# carrying 17 MB of the same JPEGs twice buys nothing. art/creation/README.md stays,
# because it is what says where they came from and how to take more.
art/creation/*.jpg
